import SwiftUI

struct ThreadInventoryView: View {
    var body: some View {
        Text("Thread Inventory")
            .navigationTitle("Thread Inventory")
    }
}

#Preview {
    NavigationView {
        ThreadInventoryView()
    }
} 